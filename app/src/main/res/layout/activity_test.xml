<?xml version="1.0" encoding="utf-8"?>

<!-- generated w/ chatgpt -> simple UI wanted to test that devices can discover and connect w/ each other -->
<!-- temp UI for testing -->

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"> <!-- takes full height of screen -->
    <!-- simple xml for ui-->
    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/btnDiscover"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/discover_devices" /> <!-- button when pressed will scan for devices. button has id: btnDiscover. -->
        <Button
            android:id="@+id/btnWait"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/receive_connections" /> <!-- button when pressed will scan for devices. button has id: btnDiscover. -->

        <ListView
            android:id="@+id/deviceListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:dividerHeight="1dp"/>
        <TextView
            android:id="@+id/tvLogs"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="vertical"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>